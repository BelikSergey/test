const currentColor = 'CHANGE_CURRENT_COLOR';
const hexValue = 'CHANGE_HEX_VALUE';
const colorModal = 'CHANGE_COLOR_MODAL';
const rgbModal = 'CHANGE_RGB_MODAL';

const types = {
  currentColor,
  hexValue,
  colorModal,
  rgbModal,
};

// eslint-disable-next-line import/no-anonymous-default-export
export default types;
