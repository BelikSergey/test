const getModalColor = state => state.colorModal;
const getHexModal = state => state.hexModal;
const getHexValue = state => state.hexValue;
const getCurrentColor = state => state.currentColor;

// eslint-disable-next-line import/no-anonymous-default-export
export default {
  getModalColor,
  getHexModal,
  getHexValue,
  getCurrentColor,
};
